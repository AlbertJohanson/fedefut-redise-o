---

---

<section class="news-fg" id="news">
  <div class="news-fg-decor">
    <div class="news-fg-decor-line news-fg-decor-line-1"></div>
    <div class="news-fg-decor-line news-fg-decor-line-2"></div>
    <div class="news-fg-decor-circle"></div>

    <div
      class="news-fg-field-element news-fg-field-corner news-fg-field-corner-tl"
    >
    </div>
    <div
      class="news-fg-field-element news-fg-field-corner news-fg-field-corner-br"
    >
    </div>

    <div class="news-fg-geo news-fg-geo-1"></div>
    <div class="news-fg-geo news-fg-geo-2"></div>
    <div class="news-fg-geo news-fg-geo-3"></div>

    <div class="news-fg-stripe news-fg-stripe-1"></div>
    <div class="news-fg-stripe news-fg-stripe-2"></div>
  </div>

  <div class="news-container">
    <div class="news-header">
      <div class="news-header-content">
        <span class="section-subtitle">Mantente Informado</span>
        <h2 class="section-title">Últimas Noticias</h2>
      </div>
      <a href="#" class="news-ver-todas">
        Ver todas las noticias
        <svg
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <path d="M5 12h14M12 5l7 7-7 7"></path>
        </svg>
      </a>
    </div>

    <div class="news-layout">
      <!-- Featured News - Large Carousel -->
      <div class="news-featured">
        <div class="featured-carousel" id="featuredCarousel">
          <!-- Slide 1 -->
          <article class="featured-slide active">
            <div class="featured-image">
              <div class="featured-placeholder">
                <img
                  src="https://res.cloudinary.com/ajweb/image/upload/v1769214399/Fedefut/GRUPO-C-PNG_txy3pl.png"
                />
              </div>
              <div class="featured-overlay"></div>
            </div>
            <div class="featured-content">
              <div class="featured-meta">
                <span class="featured-category">Juveniles</span>
                <span class="featured-date">
                  <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"
                    ></rect>
                    <line x1="16" y1="2" x2="16" y2="6"></line>
                    <line x1="8" y1="2" x2="8" y2="6"></line>
                    <line x1="3" y1="10" x2="21" y2="10"></line>
                  </svg>
                  23 Enero 2026
                </span>
              </div>
              <h3 class="featured-title">
                SELECCIÓN SUB 17 FEMENINA – CALENDARIO OFICIAL RONDA UNO
                CLASIFICATORIAS DE CONCACAF 2026
              </h3>
              <p class="featured-excerpt">
                CALENDARIO OFICIAL RONDA UNO CLASIFICATORIAS DE CONCACAF SUB 17
                FEMENINA 2026
              </p>
              <a href="#" class="featured-link">
                Leer noticia completa
                <span class="link-arrow">
                  <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7"></path>
                  </svg>
                </span>
              </a>
            </div>
          </article>

          <!-- Slide 2 -->
          <article class="featured-slide">
            <div class="featured-image">
              <div class="featured-placeholder featured-placeholder-2">
                <img
                  src="https://res.cloudinary.com/ajweb/image/upload/v1769214400/Fedefut/PORTADA-FEMENINA-PNG-1024x683_hgoref.png"
                />
              </div>
              <div class="featured-overlay"></div>
            </div>
            <div class="featured-content">
              <div class="featured-meta">
                <span class="featured-category">Juveniles</span>
                <span class="featured-date">
                  <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"
                    ></rect>
                    <line x1="16" y1="2" x2="16" y2="6"></line>
                    <line x1="8" y1="2" x2="8" y2="6"></line>
                    <line x1="3" y1="10" x2="21" y2="10"></line>
                  </svg>
                  23 Enero 2026
                </span>
              </div>
              <h3 class="featured-title">
                NÓMINA OFICIAL SELECCIÓN SUB 17 FEMENINA – CLASIFICATORIO U17
                FEMENINO DE CONCACAF
              </h3>
              <p class="featured-excerpt">
                NÓMINA OFICIAL SELECCIÓN SUB 17 FEMENINA CLASIFICATORIO U17
                FEMENINO DE CONCACAF
              </p>
              <a href="#" class="featured-link">
                Leer noticia completa
                <span class="link-arrow">
                  <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7"></path>
                  </svg>
                </span>
              </a>
            </div>
          </article>
        </div>

        <!-- Carousel Controls -->
        <div class="featured-controls">
          <div class="featured-dots" id="featuredDots">
            <button class="featured-dot active" data-slide="0"></button>
            <button class="featured-dot" data-slide="1"></button>
          </div>
          <div class="featured-arrows">
            <button
              class="featured-arrow featured-prev"
              id="featuredPrev"
              aria-label="Anterior"
            >
              <svg
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
              >
                <path d="M15 18l-6-6 6-6"></path>
              </svg>
            </button>
            <button
              class="featured-arrow featured-next"
              id="featuredNext"
              aria-label="Siguiente"
            >
              <svg
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
              >
                <path d="M9 18l6-6-6-6"></path>
              </svg>
            </button>
          </div>
        </div>
      </div>

      <div class="news-grid">
        <!-- News Card 1 -->
        <article class="news-card-mini">
          <div class="news-mini-image">
            <div class="news-mini-placeholder">
              <img
                src="https://res.cloudinary.com/ajweb/image/upload/v1769214400/Fedefut/portada-playa_bcbnos.png"
              />
            </div>
            <span class="news-mini-category">Futbol Playa</span>
          </div>
          <div class="news-mini-content">
            <span class="news-mini-date">23 Enero 2026</span>
            <h4 class="news-mini-title">
              SELECCIÓN FÚTBOL PLAYA - MICROCICLO No. 2 - 2026
            </h4>
            <a href="#" class="news-mini-link">Leer más</a>
          </div>
        </article>

        <!-- News Card 2 -->
        <article class="news-card-mini">
          <div class="news-mini-image">
            <div class="news-mini-placeholder">
              <img
                src="https://res.cloudinary.com/ajweb/image/upload/v1769214399/Fedefut/0edae6d8-0a00-47e7-8c47-f6ae209ee74e-1024x682_xmdukk.png"
              />
            </div>
            <span class="news-mini-category">Juveniles</span>
          </div>
          <div class="news-mini-content">
            <span class="news-mini-date">7 Enero 2026</span>
            <h4 class="news-mini-title">
              SELECCIÓN SUB 17 SE PREPARA PARA COMPETIR EN EL TORNEO DEL SOL
              2026
            </h4>
            <a href="#" class="news-mini-link">Leer más</a>
          </div>
        </article>

        <!-- News Card 3 -->
        <article class="news-card-mini">
          <div class="news-mini-image">
            <div class="news-mini-placeholder">
              <img
                src="https://res.cloudinary.com/ajweb/image/upload/v1769214399/Fedefut/603822686_1287050580124412_3419080293018653518_n-copia-1024x709_migbek.jpg"
              />
            </div>
            <span class="news-mini-category">Seleccion Mayor</span>
          </div>
          <div class="news-mini-content">
            <span class="news-mini-date">7 Enero 2026</span>
            <h4 class="news-mini-title">
              GUATEAMALA vs. CANADÁ - AMISTOSO INTERNACIONAL - 17 DE ENERO 2026
            </h4>
            <a href="#" class="news-mini-link">Leer más</a>
          </div>
        </article>

        <article class="news-card-mini">
          <div class="news-mini-image">
            <div class="news-mini-placeholder">
              <img
                src="https://fedefutguate.gt/wp-content/uploads/2026/01/PORTADA-FEMENINA.jpg"
              />
            </div>
            <span class="news-mini-category">Juveniles</span>
          </div>
          <div class="news-mini-content">
            <span class="news-mini-date">7 Enero 2026</span>
            <h4 class="news-mini-title">
              CONVOCATORIA SELECCIÓN SUB 17 FEMENINA – MICROCICLO No. 5
            </h4>
            <a href="#" class="news-mini-link">Leer más</a>
          </div>
        </article>
      </div>
    </div>
  </div>
</section>

<script>
  const carousel = document.getElementById("featuredCarousel");
  const prevBtn = document.getElementById("featuredPrev");
  const nextBtn = document.getElementById("featuredNext");
  const dots = document.querySelectorAll(".featured-dot");

  const slides = carousel!.querySelectorAll(".featured-slide");
  let currentIndex = 0;
  let autoPlayInterval: any;

  function showSlide(index: any) {
    // Wrap around
    if (index >= slides.length) index = 0;
    if (index < 0) index = slides.length - 1;

    currentIndex = index;

    // Update slides
    slides.forEach((slide, i) => {
      slide.classList.toggle("active", i === currentIndex);
    });

    // Update dots
    dots.forEach((dot, i) => {
      dot.classList.toggle("active", i === currentIndex);
    });
  }

  function nextSlide() {
    showSlide(currentIndex + 1);
  }

  function prevSlide() {
    showSlide(currentIndex - 1);
  }

  // Button events
  if (prevBtn) prevBtn.addEventListener("click", prevSlide);
  if (nextBtn) nextBtn.addEventListener("click", nextSlide);

  // Dot events
  dots.forEach((dot, i) => {
    dot.addEventListener("click", () => showSlide(i));
  });

  // Auto-play
  function startAutoPlay() {
    autoPlayInterval = setInterval(nextSlide, 5000);
  }

  function stopAutoPlay() {
    clearInterval(autoPlayInterval);
  }

  // Pause on hover
  carousel!.addEventListener("mouseenter", stopAutoPlay);
  carousel!.addEventListener("mouseleave", startAutoPlay);

  // Touch/swipe support
  let touchStartX = 0;
  let touchEndX = 0;

  carousel!.addEventListener(
    "touchstart",
    (e) => {
      touchStartX = e.changedTouches[0].screenX;
      stopAutoPlay();
    },
    { passive: true },
  );

  carousel!.addEventListener(
    "touchend",
    (e) => {
      touchEndX = e.changedTouches[0].screenX;
      const diff = touchStartX - touchEndX;

      if (Math.abs(diff) > 50) {
        if (diff > 0) {
          nextSlide();
        } else {
          prevSlide();
        }
      }
      startAutoPlay();
    },
    { passive: true },
  );

  // Keyboard navigation
  carousel!.setAttribute("tabindex", "0");
  carousel!.addEventListener("keydown", (e) => {
    if (e.key === "ArrowLeft") prevSlide();
    if (e.key === "ArrowRight") nextSlide();
  });

  // Start autoplay
  startAutoPlay();
</script>
<style>
  .news-fg {
    position: relative;
    padding: var(--space-24) var(--space-24);
    background: var(--color-gray-100);
    overflow: hidden;
  }

  .news-fg::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background:
      url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E"),
      linear-gradient(
        135deg,
        rgba(0, 56, 118, 0.03) 0%,
        transparent 50%,
        rgba(73, 151, 208, 0.02) 100%
      ),
      radial-gradient(
        ellipse 60% 50% at 0% 0%,
        rgba(0, 102, 204, 0.06) 0%,
        transparent 50%
      ),
      radial-gradient(
        ellipse 50% 50% at 100% 100%,
        rgba(73, 151, 208, 0.04) 0%,
        transparent 50%
      );
    opacity: 0.4;
    pointer-events: none;
  }

  .news-fg-decor {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    overflow: hidden;
  }

  .news-fg-decor-line {
    position: absolute;
    background: linear-gradient(180deg, var(--color-primary), transparent);
  }

  .news-fg-decor-line-1 {
    top: 0;
    left: 8%;
    width: 1px;
    height: 70%;
    opacity: 0.08;
  }

  .news-fg-decor-line-2 {
    top: 0;
    right: 12%;
    width: 1px;
    height: 50%;
    opacity: 0.06;
  }

  .news-fg-decor-circle {
    position: absolute;
    border-radius: 50%;
  }

  .news-fg-decor-circle::before {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 60%;
    height: 60%;
    border: 1px dashed rgba(0, 56, 118, 0.06);
    border-radius: 50%;
  }

  .news-fg-decor-circle {
    bottom: -150px;
    right: -150px;
    width: 500px;
    height: 500px;
    border: 1px solid rgba(0, 56, 118, 0.04);
  }

  .news-fg-decor::before {
    content: "";
    position: absolute;
    top: 15%;
    left: -5%;
    width: 200px;
    height: 200px;
    border: 2px solid rgba(0, 56, 118, 0.03);
    border-radius: 50%;
  }

  .news-fg-decor::after {
    content: "";
    position: absolute;
    bottom: 20%;
    left: 5%;
    width: 120px;
    height: 180px;
    border: 1px solid rgba(0, 56, 118, 0.04);
    border-left: none;
    border-radius: 0 100px 100px 0;
  }

  .news-fg-decor-line-1::before {
    content: "";
    position: absolute;
    top: 0;
    left: 50vw;
    width: 40vw;
    height: 100vh;
    background-image: radial-gradient(
      circle,
      rgba(0, 56, 118, 0.04) 1px,
      transparent 1px
    );
    background-size: 24px 24px;
    opacity: 0.8;
  }

  .news-fg-decor-line-2::before {
    content: "";
    position: absolute;
    top: 60px;
    right: 20%;
    width: 60px;
    height: 60px;
    border: 2px solid rgba(73, 151, 208, 0.08);
    transform: rotate(45deg);
  }

  .news-fg-decor-line-2::after {
    content: "";
    position: absolute;
    top: 100px;
    right: 18%;
    width: 30px;
    height: 30px;
    background: rgba(73, 151, 208, 0.04);
    transform: rotate(45deg);
  }

  .news-fg::after {
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    width: 300px;
    height: 300px;
    background: linear-gradient(
      225deg,
      rgba(0, 56, 118, 0.04) 0%,
      transparent 60%
    );
    clip-path: polygon(100% 0, 0 0, 100% 100%);
    pointer-events: none;
  }

  .news-fg-field-element {
    position: absolute;
    pointer-events: none;
  }

  .news-fg-field-corner {
    width: 80px;
    height: 80px;
    border: 1px solid rgba(0, 56, 118, 0.06);
  }

  .news-fg-field-corner-tl {
    top: 40px;
    left: 40px;
    border-right: none;
    border-bottom: none;
    border-radius: 0 0 0 40px;
  }

  .news-fg-field-corner-br {
    bottom: 40px;
    right: 40px;
    border-left: none;
    border-top: none;
    border-radius: 0 40px 0 0;
  }

  .news-fg-geo {
    position: absolute;
    pointer-events: none;
  }

  .news-fg-geo-1 {
    top: 20%;
    right: 8%;
    width: 40px;
    height: 40px;
    border: 2px solid rgba(73, 151, 208, 0.1);
    border-radius: 50%;
    animation: floatSlow 8s ease-in-out infinite;
  }

  .news-fg-geo-2 {
    top: 35%;
    right: 5%;
    width: 12px;
    height: 12px;
    background: rgba(0, 102, 204, 0.08);
    border-radius: 50%;
    animation: floatSlow 6s ease-in-out infinite reverse;
  }

  .news-fg-geo-3 {
    bottom: 30%;
    left: 3%;
    width: 24px;
    height: 24px;
    border: 2px solid rgba(0, 56, 118, 0.06);
    transform: rotate(45deg);
    animation: floatSlow 10s ease-in-out infinite;
  }

  @keyframes floatSlow {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-15px) rotate(5deg);
    }
  }

  .news-fg-geo-3 {
    animation: floatSlowRotate 10s ease-in-out infinite;
  }

  @keyframes floatSlowRotate {
    0%,
    100% {
      transform: translateY(0) rotate(45deg);
    }
    50% {
      transform: translateY(-10px) rotate(50deg);
    }
  }

  .news-fg-stripe {
    position: absolute;
    pointer-events: none;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(73, 151, 208, 0.03),
      transparent
    );
  }

  .news-fg-stripe-1 {
    top: 10%;
    left: -5%;
    width: 40%;
    height: 1px;
    transform: rotate(-5deg);
  }

  .news-fg-stripe-2 {
    bottom: 15%;
    right: -5%;
    width: 35%;
    height: 1px;
    transform: rotate(-5deg);
  }

  .news-fg-decor::before {
    content: "news-fg";
    position: absolute;
    top: 50%;
    left: -5%;
    transform: translateY(-50%) rotate(-90deg);
    font-family: var(--font-display);
    font-size: 8rem;
    letter-spacing: 0.3em;
    color: rgba(0, 56, 118, 0.02);
    white-space: nowrap;
    pointer-events: none;
  }

  .news-fg-decor::after {
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    width: 50%;
    height: 100%;
    background:
      radial-gradient(
        ellipse 80% 60% at 100% 20%,
        rgba(73, 151, 208, 0.04) 0%,
        transparent 50%
      ),
      radial-gradient(
        ellipse 60% 40% at 80% 80%,
        rgba(0, 102, 204, 0.03) 0%,
        transparent 40%
      );
    pointer-events: none;
  }

  .news-fg .news-container {
    width: 100%;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 var(--space-6);
    position: relative;
    z-index: 1;
  }

  .news-header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    margin-bottom: var(--space-12);
    gap: var(--space-6);
  }

  .news-header .section-subtitle {
    color: var(--color-secondary);
  }

  .news-header .section-title {
   color: var(--blue-400);
  }

  .news-header .section-title::after {
    margin: var(--space-4) 0 0;
  }

  .news-ver-todas {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--color-secondary);
    padding: var(--space-3) var(--space-5);
    border: 2px solid var(--color-secondary);
    border-radius: 50px;
    transition: all var(--duration-normal) var(--ease-out);
    white-space: nowrap;
  }

  .news-ver-todas svg {
    width: 18px;
    height: 18px;
    transition: transform var(--duration-normal) var(--ease-out);
  }

  .news-ver-todas:hover {
    background: var(--color-secondary);
    color: var(--color-white);
  }

  .news-ver-todas:hover svg {
    transform: translateX(4px);
  }

  .news-header::after {
    content: "";
    position: absolute;
    bottom: calc(-1 * var(--space-6));
    left: 0;
    width: 100%;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(0, 56, 118, 0.1) 20%,
      rgba(73, 151, 208, 0.15) 50%,
      rgba(0, 56, 118, 0.1) 80%,
      transparent 100%
    );
  }

  .news-header {
    position: relative;
  }

  .news-layout {
    display: grid;
    grid-template-columns: 1.4fr 1fr;
    gap: var(--space-6);
  }

  /* Featured News Carousel */
  .news-featured {
    position: relative;
    border-radius: 24px;
    overflow: hidden;
    background: var(--color-white);
    box-shadow: 0 10px 40px rgba(0, 56, 118, 0.1);
  }

  .featured-carousel {
    position: relative;
    height: 500px;
  }

  .featured-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    visibility: hidden;
    transition:
      opacity 0.6s ease,
      visibility 0.6s ease;
  }

  .featured-slide.active {
    opacity: 1;
    visibility: visible;
  }

  .featured-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .featured-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      var(--color-primary) 0%,
      var(--color-secondary) 100%
    );
  }

  .featured-placeholder img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .featured-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.1) 0%,
      rgba(0, 0, 0, 0.3) 50%,
      rgba(0, 0, 0, 0.85) 100%
    );
  }

  .featured-content {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: var(--space-8);
    color: var(--color-white);
  }

  .featured-meta {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    margin-bottom: var(--space-4);
  }

  .featured-category {
    display: inline-block;
    padding: var(--space-2) var(--space-4);
    background: var(--color-accent);
    color: var(--color-dark);
    font-family: var(--font-display);
    font-size: 0.75rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    border-radius: 4px;
  }

  .featured-date {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.7);
  }

  .featured-date svg {
    width: 16px;
    height: 16px;
  }

  .featured-title {
    font-family: var(--font-display);
    font-size: 2rem;
    line-height: 1.15;
    letter-spacing: 0.01em;
    margin-bottom: var(--space-4);
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
  }

  .featured-excerpt {
    font-size: 0.95rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: var(--space-6);
    max-width: 90%;
  }

  .featured-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--color-white);
    padding: var(--space-3) var(--space-5);
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50px;
    transition: all var(--duration-normal) var(--ease-out);
  }

  .featured-link .link-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    transition: all var(--duration-normal) var(--ease-out);
  }

  .featured-link .link-arrow svg {
    width: 14px;
    height: 14px;
  }

  .featured-link:hover {
    background: var(--color-accent);
    color: var(--color-dark);
    border-color: var(--color-accent);
  }

  .featured-link:hover .link-arrow {
    background: var(--color-dark);
    transform: translateX(4px);
  }

  .featured-link:hover .link-arrow svg {
    stroke: var(--color-accent);
  }

  /* Featured Controls */
  .featured-controls {
    position: absolute;
    bottom: var(--space-8);
    right: var(--space-8);
    display: flex;
    align-items: center;
    gap: var(--space-4);
    z-index: 10;
  }

  .featured-dots {
    display: flex;
    gap: var(--space-2);
  }

  .featured-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.4);
    border: none;
    cursor: pointer;
    transition: all var(--duration-normal) var(--ease-out);
  }

  .featured-dot.active {
    width: 24px;
    border-radius: 4px;
    background: var(--color-accent);
  }

  .featured-arrows {
    display: flex;
    gap: var(--space-2);
  }

  .featured-arrow {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-white);
    cursor: pointer;
    transition: all var(--duration-normal) var(--ease-out);
  }

  .featured-arrow svg {
    width: 20px;
    height: 20px;
  }

  .featured-arrow:hover {
    background: var(--color-accent);
    color: var(--color-dark);
    border-color: var(--color-accent);
  }

  /* Secondary News Grid */
  .news-grid {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: repeat(4, 1fr);
    gap: var(--space-4);
  }

  .news-card-mini {
    display: flex;
    gap: var(--space-4);
    background: var(--color-white);
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0, 56, 118, 0.06);
    transition: all var(--duration-normal) var(--ease-out);
  }

  .news-card-mini:hover {
    box-shadow: 0 8px 30px rgba(0, 56, 118, 0.12);
    transform: translateX(4px);
  }

  .news-mini-image {
    position: relative;
    flex: 0 0 120px;
    height: 100%;
    min-height: 110px;
  }

  .news-mini-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      var(--color-primary),
      var(--color-secondary)
    );
  }

  .news-mini-placeholder img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .news-mini-category {
    position: absolute;
    top: var(--space-3);
    left: var(--space-3);
    padding: var(--space-1) var(--space-2);
    background: var(--color-white);
    color: var(--color-primary);
    font-size: 0.45rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-radius: 3px;
  }

  .news-mini-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: var(--space-4);
    padding-left: 0;
  }

  .news-mini-date {
    font-size: 0.75rem;
    color: var(--color-gray-500);
    margin-bottom: var(--space-1);
  }

  .news-mini-title {
    font-family: var(--font-display);
    font-size: 1rem;
    letter-spacing: 0.01em;
    color: var(--color-gray-900);
    line-height: 1.25;
    margin-bottom: var(--space-2);
    transition: color var(--duration-normal) var(--ease-out);
  }

  .news-card-mini:hover .news-mini-title {
    color: var(--color-secondary);
  }

  .news-mini-link {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--color-secondary);
    transition: color var(--duration-normal) var(--ease-out);
  }

  .news-mini-link:hover {
    color: var(--color-primary);
  }

  @media (max-width: 1100px) {
    .news-layout {
      grid-template-columns: 1fr;
    }

    .featured-carousel {
      height: 450px;
    }

    .news-grid {
      grid-template-columns: repeat(2, 1fr);
      grid-template-rows: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .news-fg {
      padding: var(--space-16) 0;
    }

    .news-header {
      flex-direction: column;
      align-items: flex-start;
    }

    .news-ver-todas {
      align-self: flex-start;
    }

    .featured-carousel {
      height: 400px;
    }

    .featured-title {
      font-size: 1.5rem;
    }

    .featured-excerpt {
      display: none;
    }

    .featured-controls {
      bottom: var(--space-6);
      right: var(--space-6);
    }

    .news-grid {
      grid-template-columns: 1fr;
      grid-template-rows: auto;
    }

    .news-card-mini {
      flex-direction: row;
    }

    .news-mini-image {
      flex: 0 0 100px;
      min-height: 100px;
    }

    .ticker-label {
      padding: var(--space-3) var(--space-4);
      font-size: 0.7rem;
    }

    .news-mini-category {
      font-size: 0.25rem;
    }
  }

  @media (max-width: 480px) {
    .featured-carousel {
      height: 350px;
    }

    .featured-content {
      padding: var(--space-1);
    }

    .featured-title {
      font-size: 1.25rem;
    }

    .featured-link {
      font-size: 0.8rem;
      padding: var(--space-2) var(--space-4);
    }

    .featured-controls {
      bottom: var(--space-5);
      right: var(--space-5);
      gap: var(--space-3);
    }

    .featured-arrow {
      width: 36px;
      height: 36px;
    }

    .news-mini-image {
      flex: 0 0 80px;
    }

    .news-mini-title {
      font-size: 0.9rem;
    }
  }
</style>
